"use strict";

describe("mainFooter", function () {
  // Load the module that contains the `mainFooter` component before each test
  beforeEach(module("mainFooter"));

  // Test the controller
  describe("mainFooterController", function () {});
});
